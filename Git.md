## Configurando git por SSH Keys

Terminal:

- ssh-keygen.exe (Criar a chave ssh na maquina)
- Acessar o GitHub por exemplo:
  - Perfil -> Settings -> SSH and GPG Keys -> New SSH Key -> Adicionar titulo e
    a chave ssh gerada
